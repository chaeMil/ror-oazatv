<div class="container">
  <div class="row">
    <% @latest_videos.each do |video| %>
      <div class="col-sm-6 col-md-4 col-lg-3">
        <%= render 'video_card', video: video %>
      </div>
    <% end %>
  </div>
</div>
