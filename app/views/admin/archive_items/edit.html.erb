<div class="row toolbar">
  <div class="col">
    <h3><%= t('admin.entity.archive_item') %></h3>
  </div>
  <div class="col text-right">
    <buton class="btn btn-secondary"
           onclick="javascritp:history.back();">
      <%= t('admin.action.back') %>
    </buton>
    <a href="#" data-submit-form="archive_item_form"
       class="btn btn-success">
      <%= t('admin.action.save') %>
    </a>
  </div>
</div>

<%= render 'form', archive_item: @archive_item %>
