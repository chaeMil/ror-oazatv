<% @languages.each do |language| %>
  <div class="form-group row">
    <div class="col">
      <%= label_tag "#{name}_#{language['locale']}", "#{name.capitalize} (#{language['locale'].upcase})", class: 'col-form-label' %>
    </div>
    <div class="col-10">
      <%= text_field_tag "#{model.class.name.underscore}[#{name}_#{language['locale']}]",
                         model.send("#{name}_#{language['locale']}"),
                         class: 'form-control' %>
    </div>
  </div>
<% end %>